import 'package:flutter/material.dart';

class TaskGroupScreen extends StatelessWidget {
  const TaskGroupScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("ИКИ"),
      ),
    );
  }
}
